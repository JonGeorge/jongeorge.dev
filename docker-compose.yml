version: "3"

services:
  jongeorge:
    build:
      context: .
      dockerfile: ./Dockerfile
    networks:
      - save-a-playlist_web-server

networks:
  save-a-playlist_web-server:
    external: true